# Copy this file to .env and set your real API key there (do NOT commit .env)
# Do NOT store real secrets in this example file. Replace the value in a local `.env` file.
SARVAM_API_KEY=your_real_api_key_here

